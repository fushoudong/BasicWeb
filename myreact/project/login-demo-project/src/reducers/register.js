import * as con from '../constants'

const register = (state = {}, action) => {
  switch(action.type) {
    case con.ADD_USER:
      return state
    default:
      return state
  }
}
export default register